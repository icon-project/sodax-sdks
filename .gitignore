.DS_Store
node_modules
.turbo
*.log
dist
dist-ssr
*.local
.env*
.cache
*.tsbuildinfo
server/dist
public/dist
.history
.idea
**/build/**
**/*.testnet.ts
**/artifacts/**

# Claude Code local settings
.claude/settings.local.json
.claude/docs/
.claude/worktrees

# Agent skills (local to VSCode)
.agents/
.github/skills/

scripts/find_malicious_npm_packages.sh
.pnpm-store/
skills-lock.json

# In-progress package-version recovery state
.changeset/.version-packages-state.json*

# Local tarballs from `pnpm pack:local`
pack-local-out/

# Temp git fixtures from scripts/check-docs-drift.test.mjs
.tmp-docs-drift-*/

# Local SDK audit scratch — the shared Audits/ reports are tracked; the sdk/ subdir stays local.
Audits/sdk
