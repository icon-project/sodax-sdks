---
"@sodax/types": minor
---

Promote Robinhood Chain to the production solver environment in `swapSupportedTokens`. `ETH`, `bnUSD`, `SODA`, and `USDG` move out of `stagingSwapSupportedTokens` and are now swappable in production, matching the Hedera promotion.
