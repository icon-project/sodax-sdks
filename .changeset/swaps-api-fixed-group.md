---
"@sodax/swaps-api": patch
---

Bring `@sodax/swaps-api` into the version-aligned publish group so it releases in lockstep with the other `@sodax/*` packages under the unified `@sdks@<version>` tag. Its baseline is aligned to the current release line; no runtime or API changes.
