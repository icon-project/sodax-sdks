// Bridge API demo page — uses ONLY the Bridge API (+ allowance, sign & broadcast, minor utils).
//
// TODO(gh-255): implement. Reference: apps/demo/src/pages/swaps-api/page.tsx
// const [orders, setOrders] = useState<BridgeApiOrder[]>([]);
// render orders.map(o => <OrderStatus order={o}/>) above <BridgeCard setOrders={setOrders}/>;
// export default BridgeApiPage.
// Wiring TODO: add route { path: '/bridge-api', element: <BridgeApiPage/> } to apps/demo/src/App.tsx
// and a nav link { to: '/bridge-api', label: 'Bridge API' } in components/shared/header.tsx.

export {};
