// SelectChain — chain picker for the bridge-api demo.
//
// TODO(gh-255): implement. Reference: apps/demo/src/components/swaps-api/SelectChain.tsx
// Copy verbatim; source chains from config (sodax.config) when tokens stay client-side,
// or from the API token-map keys if a /bridge/tokens endpoint is used (Open Q #4).
// Display via chainIdToChainName (try/catch).

export {};
