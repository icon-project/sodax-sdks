// OrderStatus — polls bridge submit-tx status for one order.
//
// TODO(gh-255): implement. Reference: apps/demo/src/components/swaps-api/OrderStatus.tsx
// export type BridgeApiOrder = { txHash: string; srcChainKey: string; apiBaseURL: string };
// apiConfig = useMemo(() => ({ baseURL: order.apiBaseURL }), [order.apiBaseURL]);
// poll useBridgeApiSubmitTxStatus({ params: { txHash, srcChainKey, apiConfig } });
// render status + result.dstIntentTxHash + failedAtStep/failureReason/userMessage.

export {};
