Read and follow `AGENTS.md` before making changes.

Use the nearest package or app `AGENTS.md` for domain-specific rules.

Do not duplicate repo architecture, package lists, chain counts, or code style rules here; `AGENTS.md` is the canonical shared agent guidance.
